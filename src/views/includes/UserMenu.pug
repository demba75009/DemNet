div(class="search-menu d-flex flex-column")
  if users && users.length
    each user in users
      a(class="d-flex flex-row align-items-center search-menu-item pointer-cursor" href="/User/" + user.pseudo )
        img(width="40" height="40" class="p-1" src=user.avatar)
        span(class="pl-3")= user.pseudo
  else
    span(class="p-3") Aucun utilisateur